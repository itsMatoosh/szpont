import { MapView } from '@/components/map-view/map-view.component';

/** Map screen: full-screen map. Location is enforced app-wide before reaching tabs. */
export default function MapScreen() {
  return <MapView />;
}
